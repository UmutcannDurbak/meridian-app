<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App lock (FR-603) — BiometricPrompt via local_auth. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- Local notifications — Android 13+ requires this to be requested at
         runtime too; declaring it here is what makes the runtime request
         legal. Deliberately not requesting SCHEDULE_EXACT_ALARM — see
         notification_channel_io.dart on why these are scheduled inexactly. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Scan capture (DocumentExtraction.kt) — image_picker_android declares
         this transitively too, but it's declared explicitly here rather than
         relying on a manifest merge from a dependency, since it's a real
         capability this app uses, not incidental to one. required="false"
         because a device with no camera should still be able to use the
         app via "Choose from library" / manual entry. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <application
        android:label="meridian"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package-visibility (Android 11+, API 30+): without this,
             PackageManager can't see any camera app to resolve
             ACTION_IMAGE_CAPTURE against, and image_picker's "Take photo"
             silently fails to find one on some devices/OEM skins. -->
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE"/>
        </intent>
    </queries>
</manifest>
